{
  "name": "@repo/ws",
  "version": "1.0.0",
  "description": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "tsc && pnpm dlx tsc-alias",
    "dev": "pnpm run build && cross-env NODE_ENV=development nodemon ./JS/server/ws-server.js",
    "start:prod": "pnpm run build && cross-env NODE_ENV=production node ./JS/server/ws-server.js",
    "lint": "eslint .",
    "check-types": "tsc --noEmit"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/zod-schemas": "workspace:*",
    "cross-env": "^10.0.0",
    "socket.io": "^4.8.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  }
}
